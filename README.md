# 50+ גוונים של חלבון — Amira Pro

ספר מתכונים אינטראקטיבי. קובץ אחד: `index.html` (כולל את כל 50 התמונות בפנים).

## פרסום ראשוני (פעם אחת)
פותחים טרמינל בתיקייה הזו ואומרים ל-Claude Code:

> תפרסם את האתר הזה ב-GitHub Pages. תיצור ריפו חדש בשם amira-recipes, תעשה commit ו-push, ותפעיל Pages.

זהו. מקבלים קישור קבוע בסגנון `https://USERNAME.github.io/amira-recipes/`

## עדכון מתכון (כל פעם שרוצים)
אומרים ל-Claude Code, לדוגמה:

> תוסיף מתכון חדש לקובץ index.html: "חביתת ירק עשירה", פרק 1,
> קלוריות 250, חלבון 22, פחמימה 8, שומן 12, סיבים 2.
> מצרכים: 2 ביצים, קישוא מגורד, בצל ירוק.
> הכנה: מערבבים הכל, מטגנים על מחבת. והתמונה מצורפת בקובץ omelette.jpg — תטמיע אותה.
> אחרי זה commit + push.

תוך דקה האתר מתעדכן **לכל מי שנכנס לקישור**.

## איפה הנתונים יושבים
כל המתכונים מוגדרים בתוך `index.html` במשתנה `DEFAULT_RECIPES`, והתמונות במשתנה `IMAGES` (base64, ממופה לפי מספר מתכון). Claude Code יודע לערוך אותם ישירות.

## מצב מנהל בדפדפן (עריכה מקומית)
עדיין קיים (סיסמה: amira2025) — שינויים דרכו נשמרים רק בדפדפן שבו נעשו. לעדכון שכולם יראו — דרך Claude Code + push כמו למעלה.
